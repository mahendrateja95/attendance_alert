# =========================
# Face Recognition System - CPU Optimized Requirements
# Based on app.py dependencies analysis
# =========================

# ---- Core Web Framework ----
Flask==3.0.0
Werkzeug==3.0.1

# ---- Computer Vision & Image Processing ----
opencv-contrib-python==4.8.1.78    # cv2 for image processing
Pillow>=10.0.0                      # PIL for image handling
scikit-image>=0.21.0                # skimage.feature.local_binary_pattern

# ---- Deep Learning (PyTorch CPU) ----
torch==2.1.0                        # CPU-only PyTorch
torchvision==0.16.0                 # torch.nn.functional
facenet-pytorch==2.5.3              # MTCNN, InceptionResnetV1

# ---- Vector Search & Similarity ----
faiss-cpu==1.7.4                    # FAISS for face embedding search

# ---- Data Processing ----
numpy>=1.24.0,<2.0.0               # Core numerical operations (pinned for compatibility)
scipy>=1.11.0                       # Scientific computing utilities

# ---- Optional: MediaPipe for Face Mesh ----
mediapipe==0.10.8                   # Face mesh for eye detection
protobuf==3.20.3                    # Required for MediaPipe

# ---- Optional: InsightFace (if available) ----
# insightface==0.7.3                # Optional face recognition library
# onnx==1.15.0                      # Required for InsightFace
# onnxruntime==1.23.1               # CPU runtime for ONNX

# ---- Core Python Libraries (usually included) ----
# os, cv2, json, time, base64, secrets, hashlib, sqlite3, threading, warnings
# io, datetime, collections - these are built-in Python modules

# ---- Security & Utilities ----
# No additional cryptography libraries needed - using built-in hashlib

# ---- Development & Testing (Optional) ----
# pytest>=7.4.0                     # For testing
# requests>=2.31.0                  # For API testing